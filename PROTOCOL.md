# USB-протокол LayoutBeep ↔ RP2040-Zero

Обмен идёт через USB CDC строковыми командами, заканчивающимися переводом строки.

## Проверка платы

ПК отправляет:

```text
?
```

Плата отвечает строкой идентификации семейства LayoutBeep.

## Раскладка

```text
E0
R0
E1
R1
```

- `E0` — установить English без звука и вспышки;
- `R0` — установить Русский без звука и вспышки;
- `E1` — переключение на English: цвет + вспышка + звук;
- `R1` — переключение на Русский: цвет + вспышка + звук.

## Конфигурация

```text
C,engHz,engMs,engR,engG,engB,rusHz,rusMs,rusR,rusG,rusB,flashMs,flashR,flashG,flashB
```

Пример:

```text
C,1450,75,0,0,28,850,75,28,0,0,80,90,90,90
```

## День / ночь

```text
N,active,dayVolumePct,nightVolumePct,nightBrightnessPct
```

Пример:

```text
N,1,70,20,15
```

Означает:

- сейчас активен ночной профиль;
- дневная громкость — 70%;
- ночная громкость — 20%;
- ночная яркость ENG/РУС и вспышки — 15%.

Оранжевый offline-индикатор не масштабируется ночной яркостью.

## Heartbeat

```text
K
```

Используется для контроля связи с Windows-программой.
